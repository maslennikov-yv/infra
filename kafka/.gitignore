# Артефакты сервиса (генерируются через make, не коммитятся)
#
# - `images/*.tar` создаётся командами:
#   - из корня: `make images-save ENV=<env> [SERVICE=kafka]`
#   - из директории сервиса: `make images-save ENV=<env>`
images/*.tar

# Директория с meta-бэкапами Kafka (topic definitions + ACL + SCRAM users list)
backups/
*.tar.gz

# Временные файлы
*.swp
*.swo
*~

