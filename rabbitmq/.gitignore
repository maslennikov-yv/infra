# Артефакты сервиса (генерируются через make, не коммитятся)
#
# - `images/*.tar` создаётся командами:
#   - из корня: `make images-save ENV=<env> [SERVICE=rabbitmq]`
#   - из директории сервиса: `make images-save ENV=<env>`
images/*.tar

# Директория с definitions-бэкапами RabbitMQ (включая SHA256 хэши паролей пользователей)
backups/
*.json.gz

# Временные файлы
*.swp
*.swo
*~

